services:
  bot:
    image: bot
    build:
      context: .
      dockerfile: src/Services/Bot/Afonya.Bot.WebWorker/Dockerfile
    ports:
      - 5000:5000
    environment:
      - ASPNETCORE_ADMIN_CREDENTIALS="admin:admin""
    volumes:
      - data:/app/db:rw

volumes:
  data:
